{
  "buildCommand": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/vertex-os run build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "artifacts/vertex-os/dist/public",
  "framework": "vite",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}